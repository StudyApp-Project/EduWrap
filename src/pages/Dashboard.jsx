export default function Dashboard() {
  return (
    <div>
      <h1 style={{ color: 'var(--text-primary)', marginBottom: 8 }}>Dashboard</h1>
      <p style={{ color: 'var(--text-secondary)' }}>Phase 2 will build this screen.</p>
    </div>
  );
}
